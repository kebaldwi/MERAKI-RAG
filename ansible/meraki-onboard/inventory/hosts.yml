---
# inventory/hosts.yml
# Switches are grouped by onboarding mode.
all:
  children:
    cloud_config:
      hosts:
        cs-access-01:
          ansible_host: 192.168.1.10
        cs-access-02:
          ansible_host: 192.168.1.11

    device_config:
      hosts:
        cs-core-01:
          ansible_host: 192.168.1.20
